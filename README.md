# MAD101 Toolkit for Casio fx-CG50

Build-ready source project for a future `MAD101.g3a` add-in using fxSDK and gint.

The temporary export workflow reuses the successful fxSDK cache to verify that the calculator cross-compiler can be packaged for another add-in build.
